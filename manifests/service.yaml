apiVersion: v1
kind: Service
metadata:
  name: cricket-insight-svc
  namespace: cricket-insight
spec:
  selector:
    app: cricket-insight
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort
